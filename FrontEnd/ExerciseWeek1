const names = ["Lars", "Jan", "Peter", "Bo", "Frederik", "Jens"]

function namesFunc(){
    
    console.log.array.forEach(element => {
        
    (names.filter(names.length<=3))});
    
}

namesFunc();
console.log(names);